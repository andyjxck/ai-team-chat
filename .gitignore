# deps
node_modules/
.pnp.*
.pnpm-store/

# next
.next/
out/
build/
next-env.d.ts
tsconfig.tsbuildinfo

# env
.env
.env.local
.env*.local

# db (local dev SQLite — no longer used but just in case)
db/*.db
db/*.db-journal

# workspace (agent file I/O sandbox)
workspace/

# os
.DS_Store
*.log

# drizzle
db/migrations/

# ios (Capacitor — keep the ios/ folder but ignore build artifacts)
ios/App/build/
ios/App/DerivedData/
ios/App/App.xcodeproj/project.xcworkspace/xcuserdata/
ios/App/App.xcodeproj/xcuserdata/
