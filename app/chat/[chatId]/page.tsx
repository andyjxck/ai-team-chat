// This page is intentionally minimal — the ChatManager in the layout
// handles fetching and rendering chat data client-side to avoid full page reloads.
export default function ChatPage() {
  return null;
}
